# .libcamera_options

# 画像の解像度
--width 4056  # 横幅
--height 3040 # 縦幅

# 撮影時の回転設定
--rotation 180 # 180度回転

# シャープネス設定
--sharpness 0  # シャープネスの値（0〜100）

# 明るさ設定
--brightness 1.0 # 明るさの値（0.0〜1.0）

# コントラスト設定
--contrast 1.0  # コントラストの値（0.0〜1.0）

# ゲイン設定
--gain 1.0      # ゲインの値（0.0〜1.0）

# 彩度設定
--saturation 1.0 # 彩度の値（0.0〜1.0）

# 露出補正設定
--ev 0         # 露出補正値（-10.0〜10.0）

# 撮影タイムアウト設定
--timeout 1000 # タイムアウトの値（ミリ秒）

# HDRオプションを有効にする
--hdr          # HDRモードを有効にする

# メタデータの設定
--metadata "撮影日時: $(date)" # 撮影日時のメタデータ
